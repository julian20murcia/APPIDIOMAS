# LingoVerse - Flutter con assets reales

Esta versión usa imágenes separadas como assets dentro del código Flutter. No usa pantallazos completos como fondo.

## Montaje
Copia `lib/`, `assets/` y `pubspec.yaml` sobre tu proyecto Flutter:

```powershell
flutter clean
flutter pub get
flutter run
```
